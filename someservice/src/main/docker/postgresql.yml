version: '2'
services:
  someservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/someservice/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=someservice
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
